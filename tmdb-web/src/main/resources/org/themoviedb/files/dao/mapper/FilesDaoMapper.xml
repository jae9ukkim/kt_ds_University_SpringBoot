<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.themoviedb.files.dao.FilesDao">

    <insert id="insertUploadFile" parameterType="org.themoviedb.files.vo.request.UploadVO">
        INSERT INTO FILES
		( FILE_NUM
		, FILE_GROUP_ID
		, OBFUSCATE_NAME
		, DISPLAY_NAME
		, EXTEND_NAME
		, FILE_LENGTH
		, FILE_PATH)
		VALUES
		( (SELECT NVL(MAX(FILE_NUM), 0) + 1
		     FROM FILES
		    WHERE FILE_GROUP_ID = #{fileGroupId}) 
		, #{fileGroupId}
		, #{obfuscateName}
		, #{displayName}
		, #{extendName}
		, #{fileLength}
		, #{filePath})
    </insert>

    <select id="selectFileByGroupIdAndFileNum" parameterType="org.themoviedb.files.vo.request.SearchFileVO" resultType="org.themoviedb.files.vo.response.DownloadVO">
        SELECT DISPLAY_NAME
			 , EXTEND_NAME
			 , FILE_LENGTH
			 , FILE_PATH
          FROM FILES
         WHERE FILE_GROUP_ID = #{fileGroupId}
           AND FILE_NUM = #{fileNum}
    </select>
</mapper>