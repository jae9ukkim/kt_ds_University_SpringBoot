<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.themoviedb.actor.dao.ActorDao">
    <insert id="insertNewActor" parameterType="org.themoviedb.actor.vo.request.WriteVO">
        INSERT INTO ACTOR
		( ACTOR_ID
		, ACTOR_NAME
		, ACTOR_PROFILE_URL)
		VALUES
		( 'AC-' || TO_CHAR(SYSDATE, 'YYYYMMDD-') || LPAD(SEQ_ACTOR_PK.NEXTVAL, 6, '0')
		, #{actorName}
		, #{actorProfileUrl})
    </insert>
</mapper>