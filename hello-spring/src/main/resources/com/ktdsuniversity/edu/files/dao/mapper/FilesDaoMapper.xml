<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.ktdsuniversity.edu.files.dao.FilesDao">
    
        <insert id="insertAttachFile" parameterType="com.ktdsuniversity.edu.files.vo.request.UploadVO">
            INSERT INTO FILES
			( FILE_NUM
			, FILE_GROUP_ID
			, OBFUSCATE_NAME
			, DISPLAY_NAME
			, EXTEND_NAME
			, FILE_LENGTH
			, FILE_PATH)
			VALUES
			( (SELECT NVL(MAX(FILE_NUM), 0) + 1
                 FROM FILES
                WHERE FILE_GROUP_ID = #{fileGroupId})
			, #{fileGroupId}
			, #{obfuscateName}
			, #{displayName}
			, #{extendName}
			, #{fileLength}
			, #{filePath})
        </insert>
        
        <select id="selectFilesByGroupIdAndFileNum" parameterType="com.ktdsuniversity.edu.files.vo.request.SearchFileVO" resultType="com.ktdsuniversity.edu.files.vo.response.DownloadVO">
            SELECT DISPLAY_NAME
			     , EXTEND_NAME
			     , FILE_LENGTH
			     , FILE_PATH
			  FROM FILES 
			 WHERE FILE_GROUP_ID = #{fileGroupId}
			   AND FILE_NUM = #{fileNum}
        </select>

        <select id="selectFilePathByFileGroupId" resultType="string" parameterType="string">
	        SELECT FILE_PATH
			  FROM FILES 
			 WHERE FILE_GROUP_ID = #{id}
		</select>
		
        <select id="selectFilePathByFileGroupIdAndFileNums" resultType="string" parameterType="com.ktdsuniversity.edu.files.vo.request.SearchFileGroupVO">
	        SELECT FILE_PATH
			  FROM FILES 
			 WHERE FILE_GROUP_ID = #{fileGroupId}
			   AND FILE_NUM IN
			   <foreach collection="deleteFileNum" item="fileNum" open="(" close=")" separator=",">
			   #{fileNum} 
			   </foreach>
		</select>
        
        <delete id="deleteFilesByFileGroupId" parameterType="string">
            DELETE 
              FROM FILES 
             WHERE FILE_GROUP_ID = #{fileGroupId}
        </delete>
        
        <delete id="deleteFilesByFileGroupIdAndFileNums" parameterType="com.ktdsuniversity.edu.files.vo.request.SearchFileGroupVO">
            DELETE 
              FROM FILES 
             WHERE FILE_GROUP_ID = #{fileGroupId}
               AND FILE_NUM IN
               <foreach collection="deleteFileNum" item="fileNum" open="(" close=")" separator=",">
               #{fileNum} 
               </foreach> 
        </delete>
        
        <select id="selectNewFileGroupId" resultType="string">
            SELECT 'FG' || TO_CHAR(SYSDATE, 'YYYYMMDD-') || LPAD( SEQ_FILES_GROUP_PK.NEXTVAL, 6, '0')
              FROM DUAL
        </select>
        
        <insert id="insertFileGroupId" parameterType="string">
            INSERT INTO FILES_GROUP
			(FILE_GROUP_ID)
			VALUES
			(#{_parameter})
        </insert>
    </mapper>
    